# Poly UI landing page

Marketing site for [Poly UI](https://github.com/monolabsdev/poly-ui), built with React, TypeScript, Vite, and Tailwind CSS.

```bash
bun install
bun run dev
```

Validation:

```bash
bun test
bun run lint
bun run build
bun run preview
```
